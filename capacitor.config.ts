import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'helios.10th.anniversary',
  appName: 'helios-10th-anniversary',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
